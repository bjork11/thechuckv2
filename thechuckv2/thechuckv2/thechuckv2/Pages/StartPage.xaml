<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="thechuckv2.Pages.StartPage"
             xmlns:local="clr-namespace:thechuckv2.Resources"
             xmlns:ViewModels="clr-namespace:thechuckv2.ViewModels"
             xmlns:c="http://xamarin.com/schemas/2014/forms">

    <c:ContentPage.BindingContext>
        <ViewModels:StartPageViewModel/>
    </c:ContentPage.BindingContext>
    
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Welcome to CHUCK NORRIS fanclub!"
                TextColor="{StaticResource Primary}" 
                HorizontalOptions="Center"
                FontSize="Title"
                FontAttributes="Bold"
            />
            <Image Scale="0.5"
                Source="{local:ImageResource thechuckv2.Resources.Images.chuck.jpeg}"
            />
            <!--<RefreshView Command="{Binding GetCategoriesCommand}" 
                 BackgroundColor="{StaticResource PrimaryColor}">

                <CollectionView ItemsSource="{Binding }">
                <CollectionView.ItemsLayout>
                    <LinearItemsLayout Orientation="Vertical" ItemSpacing="2"/>
                </CollectionView.ItemsLayout>
                </CollectionView>
            </RefreshView>-->

        </StackLayout>
    </ContentPage.Content>
</ContentPage>